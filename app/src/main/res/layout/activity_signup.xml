<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bac_img"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="org.trends.trendingapp.activities.SignupActivity">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="250dp"
        android:src="@drawable/tw_logo"
        android:layout_centerHorizontal="true"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Login via Facebook"
        android:id="@+id/facebookd"
        android:layout_gravity="center_horizontal"
        android:background="#cc4fc8ed"
        android:layout_margin="8dp"
        android:layout_above="@+id/skipLogin"
        android:textColor="#ffffffff" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Skip Login"
        android:id="@+id/skipLogin"
        android:background="@android:color/transparent"
        android:layout_margin="8dp"
        android:layout_alignParentBottom="true"
        android:textColor="#ccffffff"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>
